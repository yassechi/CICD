name: expressions workflow
run-name: "${{github.actor}} #${{github.run_number}}"

env:
  RUN_NUMBER: ${{ github.run_number == 1 && 'run 1er fois' ||'run plus qu une fois'}}
  TESTING_VALUE: "Bonjour je m appelle Yassine..."

on:
  workflow_dispatch:

permissions:
  issues: read

jobs:
  learn_expression:
    runs-on: ubuntu-latest
    steps:

      # - name: "get status run number"
      #   run: echo ${{env.RUN_NUMBER}}
      # - name: "testing value"
      #   env:
      #     RESULT: ${{ contains(env.TESTING_VALUE, 'Yass') && 'contient bien Yass ..' || 'Ne contient pas !!' }}
      #   run: echo ${{env.RESULT}}

      - name: "simulation Error"
        run: exit 1

      - name: "create issue"
        if: faiure()
        run: |
          curl -L \
          -X POST \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer ${{secrets.github_token}}" \
          -H "X-GitHub-Api-Version: 2026-03-10" \
          ${{vars.API_YASS}} \
          -d '{"title":"Found a bug","body":"I'\''m having a problem with this."}'
