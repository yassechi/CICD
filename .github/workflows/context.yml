name: basic context
run-name: context running by ${{github.actor}}

on: [workflow_dispatch]

jobs:
  test_external_trigger:
    runs-on: ubuntu-latest
    steps:
      - name: "show context 'github'"
        #run: echo " => show context 'github' ${{github.actor}}"
        #run: echo " => show context 'github' ${{github.event_name}}"
        #run: echo " => show context 'github' ${{job.status}}"
        #run: echo " => show  ${{toJson(github)}}"
        run: echo " => show  ${{toJson(github.event)}}"

