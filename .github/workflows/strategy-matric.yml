name: Strategy Matrix

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{matrix.os}}

    Strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: "see Hello"
        run: echo "Hello"
