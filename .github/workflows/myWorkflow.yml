name: my workflow

on:
  push:
    branches:
      main

jobs:
  my first job:
    runs-on: ubuntu-latest
    steps:
      - name: first job
        run: echo "first job"

      - name: second job
        run: echo "second job"
