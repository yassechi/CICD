name: conditions
run-name: context running by ${{github.actor}} \#${{github.run_number}}

on:
  pull_request:
    types:
      - closed

jobs:
  test_external_trigger:
    runs-on: ubuntu-latest
    steps:
      - name: "Print Hello World "
        run: echo "Hello World"

        # commit update
        # commit update

